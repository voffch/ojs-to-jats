export default function genSettings() {
  return {
    baseUrl: '',
    show: {
      ru : true,
      en : true,
      sideBySide : true
    },
  };
}

// TODO ? separate template for the show options